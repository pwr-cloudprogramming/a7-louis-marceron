{
  "name": "backend",
  "version": "1.0.50",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir build"
  },
  "devDependencies": {
    "@types/bun": "^1.0.10"
  },
  "module": "src/index.js"
}
